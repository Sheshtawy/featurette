machine:
  python:
    version: 3.6.3

  services:
    - postgresql
dependencies:
  pre:
    - pip install pipenv
  override:
    - pipenv install
    - pipenv install --dev
test:
  override:
    - pytest
