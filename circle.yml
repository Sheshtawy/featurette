machine:
  python:
    version: 3.6.3
  environment:
    - DATABASE_URL: postgresql://usr:@127.0.0.1:5432/featurette_test
dependencies:
  pre:
    - pip install pipenv
  override:
    - pipenv install
    - pipenv install --dev
test:
  override:
    - pytest
